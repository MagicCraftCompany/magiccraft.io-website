[[redirects]]
  from = "/*"
  to = "/"
  status = 200

[build]
  command = "npm run build"
  publish = "dist"

# Environment variables should be set in the Netlify dashboard
# DO NOT add actual environment variable values here
# This is just a reminder of what variables need to be set
# [template.environment]
#   VITE_SANITY_PROJECT_ID = "Sanity project ID"
#   VITE_SANITY_DATASET = "Sanity dataset name"
#   VITE_SANITY_API_VERSION = "Sanity API version"